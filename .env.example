# AS400 MCP Server Configuration
# Copy this file to .env and fill in your values

# ODBC Connection String
# Format: DRIVER={driver};SYSTEM=hostname;UID=user;PWD=password
AS400_CONNECTION_STRING=DRIVER={IBM i Access ODBC Driver};SYSTEM=your-system.example.com;UID=YOURUSER;PWD=yourpassword;CCSID=1208;EXTCOLINFO=1

# Optional: FTP Connection (for Japanese EBCDIC source upload)
# If not set, values are extracted from AS400_CONNECTION_STRING (SYSTEM/UID/PWD)
# AS400_FTP_HOST=your-system.example.com
# AS400_FTP_USER=YOURUSER
# AS400_FTP_PASSWORD=yourpassword

# Optional: Connection timeout (seconds)
# AS400_CONNECTION_TIMEOUT=30

# Optional: Default library for queries
# AS400_DEFAULT_LIBRARY=MYLIB

# Optional: Enable debug logging
# AS400_DEBUG=false
